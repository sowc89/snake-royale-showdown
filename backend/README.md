# Backend

## Running the Server

To run the server, navigate to the `backend` directory and run:

```bash
uv run uvicorn app:app --reload --port 8000
```

If port 8000 is in use, try another port like 8001.
